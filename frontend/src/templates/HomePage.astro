---
import AposArea from '@apostrophecms/apostrophe-astro/components/AposArea.astro';
import Breadcrumbs from '../components/Breadcrumbs.astro';
import { getLocale } from '@app/utils/i18n.js';

const aposData = Astro.props.aposData;
const { page, global } = aposData;
const siteTitle = global?.siteTitle || 'ApostropheCMS Demo';
const locale = getLocale(aposData);
---

<Breadcrumbs {page} />

<h1 class="home-title">
  <span class="chip">Home: {siteTitle}</span>
</h1>

<div class="layout">
  <main>
    <div class="home">
      <div class="content">
        <div class="general-content">
          <AposArea area={page.main} locale={locale} />
        </div>
      </div>
    </div>
  </main>
</div>
